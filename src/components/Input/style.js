import { StyleSheet } from "react-native";

const styles=StyleSheet.create({
    container:{
        borderWidth: 1,
        width:"100%",
        borderColor: "#ccc",
        padding: 8,
        borderRadius: 12,
        flexDirection: "row",
    },
    input:{
        outlineStyle: "none",
        width:"100%",
    }
});

export default styles;